# The proposals

One file per RFC. The name carries the area first, then the change:
`syntax-pipe-operator.md`. A file here is a proposal the language
accepted; `TEMPLATE.md` in the root is the shape a new one takes.
